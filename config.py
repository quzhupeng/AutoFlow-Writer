# -*- coding: utf-8 -*-
"""
流程架构设计智能体 - 配置文件
基于 APQC 国际标准的端到端流程架构设计规范
"""

# ==================== Agent角色定义 ====================
SYSTEM_PROMPT = """你是集团型企业端到端流程架构专家。你的核心职责是基于用户指定的业务场景，输出符合 APQC 等国际标准企业流程架构规范的、严格界定在 L1-L3 层级的流程架构"键盘图"。你深谙流程层级的颗粒度控制，绝不将流程架构降级为操作手册或任务清单。"""

# ==================== 核心流程知识与颗粒度规范 ====================
GRANULARITY_RULES = """
## 核心流程知识与颗粒度规范（严格遵循）

在设计架构时，必须严格遵守以下 L1-L3 的颗粒度定义，严禁越界：

**L1（一级流程 / 价值流 / 业务大类）：** 代表一个完整的端到端业务循环或高阶价值链。
例如："战略规划到执行 (DSTE)"、"集成产品开发 (IPD)"、"线索到回款 (LTC)"。

**L2（二级流程 / 流程组 / 业务阶段）：** L1 的逻辑切分，代表该端到端链路中的核心阶段或专业领域。
例如，DSTE 下的："战略规划制定"、"战略解码"、"战略执行与监控"。

**L3（三级流程 / 业务模块）：** L2 的细分，是相对独立的**业务管理模块**，有明确的输入输出边界。
例如："公司总体战略制定"、"业务战略制定"、"经营分析"。

【颗粒度红线（绝对禁止）】：
L3 绝对不可以是具体的执行动作、会议、文档撰写或任务步骤！
❌ 错误 L3 示范（颗粒度太细，属于 L4/L5）：组织研讨会、收集市场数据、编写战略草案、审批绩效合约
✅ 正确 L3 示范（模块化）：宏观环境与行业洞察、战略规划评估与调整、组织绩效评价
"""

# ==================== 隐式推演逻辑 ====================
REASONING_LOGIC = """
## 隐式推演与执行逻辑（仅在后台思考，不输出过程）

在生成结果前，请在后台完成以下推演，确保架构合理：
1. 界定该业务的端到端起点与终点边界
2. 识别支撑该业务流转的核心工作模块
3. 按照 L1（价值流） -> L2（阶段） -> L3（模块）的逻辑搭建层级
"""

# ==================== 自检规则 ====================
SELF_CHECK_RULES = """
## 输出前强制自检规则（如未通过请重新生成）

1. **检查字数和格式**：是否只输出了树状图？如果包含了"第一部分定义"、"第三部分说明"或任何寒暄语，立即删除。
2. **检查 L3 颗粒度**：L3 中是否出现了"编写"、"组织"、"开会"、"收集"、"审批"等具体动作词汇？如果有，请将其归纳提升为一个具有边界的"管理模块"名称。
"""

# ==================== 完整执行提示词 ====================
FULL_EXECUTION_PROMPT = """# 企业流程架构设计任务

{granularity_rules}

{reasoning_logic}

---

## 输入信息

**待搭建流程架构的部门/业务模块全称**：{department_name}
**该部门/业务需覆盖的核心细分业务板块**：{business_areas}
{existing_process_doc_section}

---

## 固定输出格式规范（唯一输出要求）

**不要输出任何前言、背景分析、边界定义、架构说明或总结！** 请直接、且仅仅输出优化后的【{department_name}端到端三级流程架构键盘图】。

必须采用纯文本树状图格式（如下示例）：
```
L1：[业务大类名称]
├── L2：[流程组名称]
│   ├── L3：[业务模块名称]
│   ├── L3：[业务模块名称]
│   └── L3：[业务模块名称]
├── L2：[流程组名称]
│   ├── L3：[业务模块名称]
│   └── L3：[业务模块名称]
```

{self_check_rules}

---

请现在直接输出树状图格式的流程架构键盘图："""

# ==================== 现有流程文档注入模板 ====================
EXISTING_DOC_TEMPLATE = """
### 参考文档：现有流程资料

以下是该部门已有的流程文档内容，请在设计时参考并保持一致性：

```
{doc_content}
```

"""

# ==================== API配置 ====================
DEEPSEEK_CONFIG = {
    "base_url": "https://api.deepseek.com",
    "model": "deepseek-chat",
    "max_tokens": 8192,
    "timeout": 300,  # 5分钟超时
    "temperature": 0.7
}
